{
  "family": "webapp-task-$ENV_VAR",
  "networkMode": "bridge",
  "executionRoleArn": "arn:aws:iam::$ACCOUNT_ID:role/ecsTaskExecutionRole-$ENV_VAR",
  "taskRoleArn": "arn:aws:iam::$ACCOUNT_ID:role/ecsTaskExecutionRole-$ENV_VAR",
  "requiresCompatibilities": ["EC2"],
  "cpu": "$APP_CPU",
  "memory": "$APP_MEMORY",
  "containerDefinitions": [
    {
      "name": "webapp",
      "image": "placeholder-image-will-be-replaced-by-ci-cd",
      "essential": true,
      "portMappings": [
        {
          "containerPort": 3200,
          "protocol": "tcp"
        }
      ],
      "environment": [
        {
          "name": "ENV",
          "value": "$ENV_VAR"
        }
      ],
      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "/ecs/webapp-$ENV_VAR",
          "awslogs-region": "us-east-1",
          "awslogs-stream-prefix": "ecs"
        }
      }
    }
  ]
}
